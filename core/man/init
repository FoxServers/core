foxservers 0.1.0
Usage: foxservers init folder path/to/.env [optional] path/to/.zip [optional] path/to/.png

init provides a simple one script setup for the server. Data is stored in /srv/foxservers/folder/. 
init must be provided with the folder and path/to/.env variables. Optionally can also be provided with a path to mods.zip and server_icon.png as defined in the .env file.
